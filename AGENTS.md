# Git 提交消息规范

Git 提交消息必须使用中文，并遵循以下格式：

```text
<类型>: <中文说明>
```

支持的提交类型：

- `feat:` 新功能
- `fix:` 修复问题
- `docs:` 文档修改
- `style:` 格式调整
- `refactor:` 代码重构
- `test:` 测试相关
- `chore:` 其他修改

示例：

```text
feat: 新增图片批量上传功能
fix: 修复图片预览无法关闭的问题
```
